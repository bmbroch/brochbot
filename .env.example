# ── Mission Control Auth ───────────────────────────────────────────────────────
# The password to access the site (set this in Vercel environment variables).
# Choose a strong password — this is the only thing protecting the site.
MC_PASSWORD=your-strong-password-here

# A long random secret used to sign the session JWT.
# Generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
MC_JWT_SECRET=your-long-random-jwt-secret-here

# ── Supabase ───────────────────────────────────────────────────────────────────
SUPABASE_CLC_URL=https://your-project.supabase.co
SUPABASE_CLC_KEY=your-supabase-anon-key

# ── Convex ─────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud
